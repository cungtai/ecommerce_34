<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse"
        data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav">
        <li class="active"><%= link_to t("pages.home"), root_path %></a></li>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">
            <%= t ".products" %>
            <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <% @catalogs.each do |cat| %>
            <li><%= link_to cat.name, catalog_products_path(cat.id) %></li>
            <% end %>
          </ul>
        </li>
        <li><%= link_to t("pages.suggest_product"), new_suggest_product_path %></li>
        <li><%= link_to t("pages.ship_cod"), pages_path("ship-cod") %></li>
        <li><%= link_to t("pages.why_us"), pages_path("why_us") %></li>
        <li><%= link_to t("pages.contact"), pages_path("contact") %></li>
      </ul>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <li>
            <%= link_to carts_path do %>
              <span class="glyphicon glyphicon-shopping-cart"></span>
              <%= t "pages.cart" %>
              <label id="cart"><%= @cart.items.size %></label>
            <% end %>
          </li>
          <% if user_signed_in? %>
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                <%= t "panel" %>
                <span class="caret"></span>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to t("users.admin_panel"), admin_users_path %></li>
                <li><%= link_to t("users.profile"), edit_user_path(current_user.id) %></li>
                <li><%= link_to t("users.change_password_title"), edit_user_password_path %></li>
                <li class="divider"></li>
                <li>
                  <%= link_to t("pages.logout"), destroy_user_session_path,
                    method: :delete %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to t("pages.sign_in"), new_user_session_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</nav>
